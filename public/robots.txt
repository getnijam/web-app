# Allow crawling of all pages. Auth/dashboard pages carry their own
# `noindex` meta, so they're intentionally left crawlable, that lets
# Google read the noindex tag and keep them out of the index.
User-agent: *
Allow: /

Sitemap: https://www.nijam.dev/sitemap.xml
