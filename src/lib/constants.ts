/**
 * Brand-level constants that more than one surface needs. Keep this small: it is for
 * facts about Nijam itself (contact addresses, canonical names), not for feature
 * configuration, which belongs beside the feature.
 */

/** The one public contact address. Used by the legal, security, and support pages. */
export const SUPPORT_EMAIL = 'support@nijam.dev';
